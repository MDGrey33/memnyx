# Workspace-local overlay

> Personal workspace preferences. Loaded natively by Claude Code alongside
> `CLAUDE.md` (no `@`-include needed); never synced, never overwritten. Yours to
> edit freely. Additive only — extends the base `CLAUDE.md`, never contradicts it.

## Conventions

<!-- Workspace-wide conventions you want loaded every session -->
- Language:
- Framework:
- Test runner:
- Formatting:
