# {{workspace_name}} — fork overlay

> Stub. The base CLAUDE.md is upstream; this overlay is for fork-specific additions.
> Memnyx ships it empty; a fork fills it in. Deployed by `/setup-workspace` only
> when the sync source is a fork (decided by git provenance, not by this file).
> Additive only — extends the base, never contradicts it.

## Fork Skills

<!-- Forks: list skills your fork adds beyond upstream. -->

## Fork-Specific Artefacts

<!-- Forks: list fork-specific docs/artefacts here. -->
